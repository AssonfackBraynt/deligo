# DeliGo API

Production-oriented NestJS backend for the DeliGo MVP.

## Current scope

- Project setup
- Environment configuration
- PostgreSQL/Prisma database layer
- JWT authentication module

## Local commands

```bash
npm install
npm run prisma:generate
npm run prisma:migrate
npm run start:dev
```

Copy `.env.example` to `.env` before running the API.
